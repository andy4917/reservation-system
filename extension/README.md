# Chrome Extension

이 폴더는 그대로 Chrome `압축해제된 확장 프로그램` 로드 대상입니다.

직접 로드:

1. `chrome://extensions`
2. `개발자 모드` 활성화
3. `압축해제된 확장 프로그램 로드`
4. 이 `extension/` 폴더 선택

패키지 산출물이 필요하면 루트에서 아래 명령을 실행합니다.

```bash
npm run extension:package
```
