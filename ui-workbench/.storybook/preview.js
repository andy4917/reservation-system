const preview = {
  parameters: {
    layout: "fullscreen",
    options: {
      storySort: {
        order: ["Desktop Program UI", "Stitch Recovery"],
      },
    },
  },
};

export default preview;
