[project]
name = "reservation-system"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = ["rapidfuzz>=3.14,<4"]

[build-system]
requires = ["setuptools>=65", "wheel", "pybind11>=2.11"]
build-backend = "setuptools.build_meta"
